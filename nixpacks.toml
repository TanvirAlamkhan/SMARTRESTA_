[providers]
providers = ["php"]

[phases.setup]
nixPkgs = [
    "php82",
    "php82Extensions.pdo",
    "php82Extensions.pdo_mysql",
    "php82Extensions.mysqli",
    "php82Extensions.mbstring",
    "php82Extensions.curl",
    "php82Extensions.openssl",
    "php82Extensions.gd"
]

[start]
cmd = "php bin/migrate.php; php -S 0.0.0.0:$PORT"
